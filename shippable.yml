language: java
jdk: oraclejdk8
install:
  - shippable_retry mvn install
stages:
  - build
  - test
  
build:
stage: build
script: "mvn package"
artifacts:
  reports:
     junit:
       - target/surefire-reports/TEST-*.xml
  
test:
  stage: test
  script:
    - echo "testing"
    - mvn test